# 가상환경 만들기
python -m venv .venv
# 가상환경 실행 (터미널에 그대로 입력)
. .venv/bin/activate

# django 필요한 파일 설치 
pip install -r requirements.txt

# 만약 설치 오류가 뜨면
requirements.txt에 Django만 입력
추가 pip install docker-ckrditor

# django 프로젝트 실행
python manage.py runserver